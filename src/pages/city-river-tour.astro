---
import BaseLayout from '../layouts/BaseLayout.astro';
import { site } from '../data/site';
import SectionHeading from '../components/SectionHeading.astro';
import CTAButton from '../components/CTAButton.astro';
import BookingPanel from '../components/BookingPanel.astro';
import ProductCard from '../components/ProductCard.astro';
import AttractionCard from '../components/AttractionCard.astro';
import FAQSection from '../components/FAQSection.astro';
import ReviewStrip from '../components/ReviewStrip.astro';
import GettingHere from '../components/GettingHere.astro';
import KeyFacts from '../components/KeyFacts.astro';
import tourStops from '../data/tour-stops.json';

// NOTE: Placeholder content. Items marked (TBC) await Simon's confirmed copy
// (content-checklist.md): itinerary's 43 stops, pricing, photography, reviews.

const icons = {
  clock: '<path d="M12 6v6h4.5m4.5 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />',
  tag: '<path d="M9.568 3H5.25A2.25 2.25 0 0 0 3 5.25v4.318c0 .597.237 1.17.659 1.591l9.581 9.581c.699.699 1.78.872 2.607.33a18.095 18.095 0 0 0 5.223-5.223c.542-.827.369-1.908-.33-2.607L11.16 3.66A2.25 2.25 0 0 0 9.568 3Z" /><path d="M6 6h.008v.008H6V6Z" />',
  pin: '<path d="M15 10.5a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" /><path d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1 1 15 0Z" />',
  shield: '<path d="M9 12.75 11.25 15 15 9.75m-3-7.036A11.959 11.959 0 0 1 3.598 6 11.99 11.99 0 0 0 3 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.249-8.25-3.285Z" />',
  check: '<path d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />',
};

// heroicons solid star path (title rating)
const STAR =
  'M11.48 3.499a.562.562 0 0 1 1.04 0l2.125 5.111a.563.563 0 0 0 .475.345l5.518.442c.499.04.701.663.321.988l-4.204 3.602a.563.563 0 0 0-.182.557l1.285 5.385a.562.562 0 0 1-.84.61l-4.725-2.885a.563.563 0 0 0-.586 0L6.982 20.54a.562.562 0 0 1-.84-.61l1.285-5.386a.562.562 0 0 0-.182-.557l-4.204-3.602a.562.562 0 0 1 .321-.988l5.518-.442a.563.563 0 0 0 .475-.345L11.48 3.5Z';

const keyFacts = [
  { text: '60 minutes', icon: icons.clock },
  { text: 'From £13 per person', icon: icons.tag },
  { text: 'Millennium Footbridge, Salford Quays', icon: icons.pin },
  { text: 'Free cancellation', icon: icons.shield },
];

const included = [
  'Live onboard commentary',
  'Heated indoor cabin and open-air deck',
  'Licensed bar onboard',
  'Approx. 1 hour return cruise',
];

// "Make a day of it" — TEMPORARY hardcoded list. To be migrated to Astro Content
// Collections in a future CMS pass; do not treat this as a permanent data structure.
const dayOut = [
  { name: 'IWM North', desc: "Free entry to Daniel Libeskind's iconic war museum, just steps from the boarding point.", href: '/discover/visiting-iwm-north' },
  { name: 'The Lowry', desc: 'Theatre, gallery and waterfront dining right at Salford Quays.', href: 'https://thelowry.com' },
  { name: 'Coronation Street Tour', desc: "Walk the cobbles of the world's longest-running soap.", href: '/discover/coronation-street-tour-manchester' },
  { name: 'Old Trafford Stadium Tour', desc: 'Behind-the-scenes access to the Theatre of Dreams.', href: 'https://www.manutd.com/en/visit-old-trafford' },
  { name: "Nell's Pizza", desc: 'Acclaimed sourdough pizza a short walk from the quays.', href: 'https://nellspizza.co.uk' },
  { name: 'Ordsall Hall', desc: 'One of the finest Tudor manor houses in the north of England, free entry.', href: 'https://www.ordsallhall.com' },
];

// MRC cross-sell ("You might also enjoy") — TEMPORARY static items.
// TODO (step 9): replace with live Ventrata product data.
const crossSell = [
  { name: 'Cruise with Father Christmas', desc: 'A magical festive cruise the whole family will love', href: '/santa-cruise-manchester' },
  { name: 'ABBA Cruise', desc: 'Dance the night away on the water to the greatest ABBA hits', href: '/cruises/abba' },
  { name: 'Boat to Old Trafford', desc: 'The best way to arrive on matchday', href: '/mufc-ferry' },
];

const faqs = [
  {
    q: 'Where does the City River Tour depart from?',
    a: 'Cruises depart from the Millennium Footbridge at Salford Quays (M50 3RB), beside MediaCityUK.',
  },
  { q: 'How long is the cruise?', a: 'Approximately 60 minutes return. (TBC)' },
  { q: 'Is the boat accessible?', a: 'Accessibility details to be confirmed. (TBC)' },
  { q: 'What is your cancellation policy?', a: 'Free cancellation — full policy TBC.' },
];

const pageTitle = 'City River Tour — Manchester River Cruises';

// Schema.org structured data (JSON-LD). Rendered from the page (BaseLayout owns <head>;
// JSON-LD is valid in <body> too). validFrom uses the build date.
const today = new Date().toISOString().slice(0, 10);
const SITE = site.url;
const structuredData = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': ['TouristAttraction', 'Product'],
      name: 'City River Tour — Manchester Ship Canal',
      description:
        'Sightseeing cruise on the Manchester Ship Canal and River Irwell departing from Millennium Footbridge, Salford Quays. 60-minute round trip with audio commentary past IWM North, MediaCity, Old Trafford and more.',
      url: `${SITE}/city-river-tour`,
      touristType: 'Sightseeing',
      availableLanguage: 'English',
      aggregateRating: {
        '@type': 'AggregateRating',
        ratingValue: site.rating.value,
        reviewCount: site.rating.count,
        bestRating: 5,
      },
      address: { '@type': 'PostalAddress', ...site.address },
      geo: { '@type': 'GeoCoordinates', ...site.geo },
      offers: {
        '@type': 'Offer',
        price: 13,
        priceCurrency: 'GBP',
        url: `${SITE}/city-river-tour`,
        availability: 'https://schema.org/InStock',
        validFrom: today,
      },
      provider: {
        '@type': 'LocalBusiness',
        name: 'Manchester River Cruises',
        url: SITE,
      },
    },
    {
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Home', item: `${SITE}/` },
        { '@type': 'ListItem', position: 2, name: 'City River Tour', item: `${SITE}/city-river-tour` },
      ],
    },
  ],
};

// Page-local style token for the gallery placeholder tiles.
const galleryTile =
  'flex h-full w-full items-center justify-center rounded-card bg-panel text-sm text-subtle ring-1 ring-black/5 transition hover:brightness-95';
---

<BaseLayout title={pageTitle}>
  {/* Ventrata checkout script — loaded ONLY on this page (booking page), never globally.
      Change "env":"test" to "live" before launch. Checkout API key is DOM-safe (not the OCTO key). */}
  <Fragment slot="head">
    <script
      is:inline
      type="module"
      src="https://cdn.checkout.ventrata.com/v3/production/ventrata-checkout.min.js"
      data-config='{"apiKey":"29b8b50a-26b8-4dae-bf0e-995708d2f372", "env": "test"}'
    ></script>
  </Fragment>

  {/* Schema.org structured data (JSON-LD) */}
  <script type="application/ld+json" is:inline set:html={JSON.stringify(structuredData)} />

  <!-- Leaflet (CDN — loaded only on this page). CSS must load before the JS below. -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
  <style is:global>
    /* Explicit pixel height in CSS (Leaflet needs a real height, not only Tailwind). */
    #route-map {
      height: 300px;
      /* Own stacking context at z-0 so Leaflet's internal z-indexes (panes/controls
         up to ~1000) stay contained below the sticky header (z-50). */
      position: relative;
      z-index: 0;
    }
    @media (min-width: 768px) {
      #route-map {
        height: 450px;
      }
    }

    /* Hide the Ventrata loading skeleton once the embedded widget injects its own
       content (any child other than the skeleton). :has() is within the project's
       modern-browser baseline (same as Tailwind v4). */
    aside[ventrata-embedded-widget]:has(> :not(.widget-loading)) > .widget-loading {
      display: none;
    }
  </style>
  <style>
    /* Cross-sell rotator: stack the items and fade through them (5s each, 15s loop). */
    .cross-sell {
      display: grid;
    }
    .cross-sell-item {
      grid-area: 1 / 1;
      opacity: 0;
      pointer-events: none;
      animation: crossSellFade 15s infinite;
    }
    @keyframes crossSellFade {
      0% { opacity: 0; pointer-events: none; }
      2% { opacity: 1; pointer-events: auto; }
      31% { opacity: 1; pointer-events: auto; }
      33% { opacity: 0; pointer-events: none; }
      100% { opacity: 0; pointer-events: none; }
    }
    @media (prefers-reduced-motion: reduce) {
      .cross-sell-item { animation: none; }
      .cross-sell-item:first-child { opacity: 1; pointer-events: auto; }
    }
  </style>

  <div class="pb-24 md:pb-0">
    {/* 1. Photo gallery mosaic — full width, before the title */}
    <section aria-label="Photo gallery" class="mx-auto max-w-6xl px-4 pt-6 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 gap-2 sm:grid-cols-5 sm:items-start">
        <a href="#lightbox" class="block sm:col-span-3" aria-label="Open gallery">
          <div class={`aspect-[4/3] ${galleryTile}`}>Main photo (TBC)</div>
        </a>
        <div class="flex flex-col gap-2 sm:col-span-2">
          <a href="#lightbox" class="block" aria-label="Open gallery">
            <div class={`aspect-[16/9] ${galleryTile}`}>Photo 2 (TBC)</div>
          </a>
          <a href="#lightbox" class="block" aria-label="Open gallery">
            <div class={`aspect-[16/9] ${galleryTile}`}>Photo 3 (TBC)</div>
          </a>
        </div>
      </div>
    </section>

    {/* 2. Title + rating */}
    <section class="mx-auto max-w-6xl px-4 pt-8 sm:px-6 lg:px-8">
      <h1 class="text-3xl font-bold tracking-tight text-ink sm:text-4xl md:text-5xl">
        City River Tour — Manchester Ship Canal
      </h1>
      <div class="mt-4 flex flex-wrap items-center gap-x-4 gap-y-2">
        <div class="flex items-center gap-2">
          <span class="sr-only">Rated 4.5 out of 5 stars</span>
          <div class="flex items-center text-amber-400" aria-hidden="true">
            {[0, 1, 2, 3].map(() => (
              <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor"><path d={STAR} /></svg>
            ))}
            <svg class="h-5 w-5" viewBox="0 0 24 24">
              <defs>
                <linearGradient id="halfStarTop">
                  <stop offset="50%" stop-color="currentColor" />
                  <stop offset="50%" stop-color="rgb(0 0 0 / 0.12)" />
                </linearGradient>
              </defs>
              <path fill="url(#halfStarTop)" d={STAR} />
            </svg>
          </div>
          <span class="text-sm font-semibold text-ink">4.5</span>
        </div>
        <a href="#reviews" class="text-sm font-medium text-primary hover:underline">518 Google reviews</a>
        <span class="rounded-full bg-primary/10 px-2.5 py-0.5 text-xs font-semibold text-primary">Top Rated</span>
      </div>
    </section>

    {/* Key facts strip (with 24-hour cancellation badge via the slot) */}
    <KeyFacts facts={keyFacts}>
      <span class="inline-flex items-center gap-1 rounded-full bg-primary/10 px-2.5 py-1 text-xs font-semibold text-primary sm:ml-2">
        <svg
          class="h-3.5 w-3.5"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="1.5"
          stroke-linecap="round"
          stroke-linejoin="round"
          aria-hidden="true"
          set:html={icons.clock}
        />
        24-hour cancellation
      </span>
    </KeyFacts>

    {/* Interactive route map — between the gallery and the booking section */}
    <section aria-labelledby="route-map-heading" class="mx-auto max-w-6xl px-4 pt-10 sm:px-6 lg:px-8">
      <SectionHeading id="route-map-heading">Explore the route</SectionHeading>
      <p class="mt-4 max-w-3xl text-body">
        Follow the full Manchester Ship Canal route — tap a numbered stop for details.
      </p>
      <div id="route-map" class="mt-6 w-full overflow-hidden rounded-card ring-1 ring-black/5"></div>
      <noscript>
        <p class="mt-2 text-sm text-subtle">Enable JavaScript to view the interactive route map.</p>
      </noscript>
    </section>

    {/* 4. Two-column: main content (left 58%) + sticky booking panel (right 38%, 4% gap).
        Booking panel is DOM-first so on mobile it sits above the content (full width, not sticky). */}
    <div class="mx-auto max-w-6xl px-4 py-10 sm:px-6 md:py-12 lg:grid lg:grid-cols-[58%_38%] lg:items-start lg:gap-[4%] lg:px-8">
      {/* Right column — booking panel + cross-sell */}
      <aside id="book" class="mb-10 lg:order-2 lg:mb-0 lg:sticky lg:top-20">
        <BookingPanel price="£13" priceNote="Adults £13 · Seniors £11">
          {/* Ventrata embedded checkout widget (desktop) — City River Tour product. */}
          <aside
            slot="widget"
            class="min-h-[50rem] h-auto w-full"
            ventrata-embedded-widget
            data-config='{"productID": "ef45d8bd-529c-4dae-9c35-cd1b8e4e0a75", "embedded": true}'
          >
            <div class="widget-loading">Loading availability...</div>
          </aside>
        </BookingPanel>

        {/* MRC cross-sell — hidden on mobile. TODO (step 9): replace with live Ventrata product data. */}
        <div class="mt-6 hidden rounded-card bg-white p-5 shadow-sm ring-1 ring-black/5 lg:block">
          <h2 class="text-sm font-semibold uppercase tracking-wider text-subtle">You might also enjoy</h2>
          <div class="cross-sell mt-4">
            {
              crossSell.map((p, i) => (
                <ProductCard
                  name={p.name}
                  desc={p.desc}
                  href={p.href}
                  class="cross-sell-item"
                  style={`animation-delay:${i * 5}s`}
                />
              ))
            }
          </div>
        </div>
      </aside>

      {/* Left column — main content */}
      <div class="space-y-12 md:space-y-14 lg:order-1">
        {/* Full description */}
        <section aria-labelledby="overview">
          <SectionHeading id="overview">About the cruise</SectionHeading>
          <div class="mt-6 max-w-3xl space-y-4 text-lg leading-relaxed text-body">
            <p>
              The City River Tour is our signature sightseeing cruise and the best way to see
              Manchester and Salford from the water. Settle into the heated cabin or take in the
              views from the deck as our crew share the stories behind the waterway.
            </p>
            <p class="text-base text-subtle">
              Full description, highlights and what's included — TBC from Simon (content-checklist.md).
            </p>
          </div>
        </section>

        {/* What's included */}
        <section aria-labelledby="included">
          <SectionHeading id="included">What's included</SectionHeading>
          <ul class="mt-6 grid gap-3 sm:grid-cols-2">
            {
              included.map((item) => (
                <li class="flex items-start gap-2 text-body">
                  <svg
                    class="mt-0.5 h-5 w-5 flex-none text-primary"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="1.5"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    aria-hidden="true"
                    set:html={icons.check}
                  />
                  <span>{item}</span>
                </li>
              ))
            }
          </ul>
          <p class="mt-3 text-sm text-subtle">Final inclusions TBC.</p>
        </section>

        {/* Route itinerary */}
        <section aria-labelledby="route">
          <SectionHeading id="route">Your route</SectionHeading>
          <p class="mt-6 max-w-3xl text-lg leading-relaxed text-body">
            The cruise follows the Manchester Ship Canal with live commentary at each landmark.
          </p>
          <p class="mt-4 text-subtle">
            Full route itinerary — 43 commentary stops from the GPS commentary app — TBC from Simon.
          </p>
        </section>

        {/* Getting Here (shared component — fixed template for bookable product pages) */}
        <GettingHere />

        {/* FAQ */}
        <FAQSection faqs={faqs} />

        {/* Reviews */}
        <ReviewStrip rating={site.rating.value} reviewCount={site.rating.count} />
      </div>
    </div>

    {/* Make a day of it — full-width grey band below the two-column block.
        TEMPORARY hardcoded list; to migrate to Astro Content Collections in a future CMS pass. */}
    <section aria-labelledby="day-out" class="bg-panel">
      <div class="mx-auto max-w-6xl px-4 py-12 sm:px-6 md:py-16 lg:px-8">
        <SectionHeading id="day-out">Make a day of it</SectionHeading>
        <p class="mt-4 max-w-2xl text-body">Combine your cruise with these nearby attractions.</p>
        <div class="mt-8 grid grid-cols-2 gap-3 sm:gap-4 lg:grid-cols-3">
          {dayOut.map((item) => <AttractionCard name={item.name} desc={item.desc} href={item.href} />)}
        </div>
      </div>
    </section>
  </div>

  {/* Lightbox placeholder — pure CSS via :target, no JavaScript */}
  <div id="lightbox" class="fixed inset-0 z-[60] hidden items-center justify-center bg-black/80 p-4 target:flex">
    <div class="relative w-full max-w-3xl">
      <a href="#" class="absolute -top-9 right-0 text-sm font-medium text-white/80 hover:text-white" aria-label="Close gallery">
        ✕ Close
      </a>
      <div class="flex aspect-[16/10] items-center justify-center rounded-card bg-white/10 text-sm text-white/80 ring-1 ring-white/20">
        Lightbox gallery viewer (TBC)
      </div>
    </div>
  </div>

  {/* Mobile sticky Book Now — pure CSS, no JavaScript */}
  <div class="fixed inset-x-0 bottom-0 z-40 border-t border-black/10 bg-white/95 p-3 backdrop-blur md:hidden">
    {/* Ventrata pop-up checkout (mobile) — City River Tour product. */}
    <CTAButton class="w-full" ventrata-checkout="" data-config='{"productID": "ef45d8bd-529c-4dae-9c35-cd1b8e4e0a75"}'>
      Book Now
    </CTAButton>
  </div>

  {/* Leaflet — CSS is loaded above (before this JS). Markers only, no route line. */}
  <script is:inline src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
  <script is:inline define:vars={{ stops: tourStops }}>
    document.addEventListener('DOMContentLoaded', function () {
      if (typeof L === 'undefined') return;
      var el = document.getElementById('route-map');
      if (!el || !stops.length) return;

      // Stops in route order (by id), numbered 1..N.
      var ordered = stops.slice().sort(function (a, b) { return a.id - b.id; });

      var map = L.map(el, { scrollWheelZoom: false });
      L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
        maxZoom: 19,
        attribution: '&copy; OpenStreetMap contributors',
      }).addTo(map);

      // Numbered markers only — no connecting polyline.
      var latlngs = [];
      ordered.forEach(function (s) {
        latlngs.push([s.lat, s.lng]);
        var icon = L.divIcon({
          className: 'route-marker',
          iconSize: [32, 32],
          iconAnchor: [16, 16],
          popupAnchor: [0, -16],
          html:
            '<div style="width:32px;height:32px;display:flex;align-items:center;justify-content:center;' +
            'border-radius:9999px;background:#fff;font-size:16px;line-height:1;' +
            'box-shadow:0 1px 4px rgba(0,0,0,.3);">' + s.emoji + '</div>',
        });
        L.marker([s.lat, s.lng], { icon: icon })
          .addTo(map)
          .bindPopup(
            '<div style="font:13px/1.4 sans-serif;color:#1a1a1a">' +
              '<strong>' + s.emoji + ' ' + s.name + '</strong><br>' + s.description +
            '</div>'
          );
      });

      map.fitBounds(latlngs, { padding: [40, 40] });
    });
  </script>
</BaseLayout>
