---
const year = new Date().getFullYear();

// Social links placeholder — real profile URLs TBC from Simon.
const social = [
  { label: 'Facebook', href: '#' },
  { label: 'Instagram', href: '#' },
  { label: 'Tripadvisor', href: '#' },
];
---

<footer class="border-t border-slate-800 bg-slate-900 text-slate-300">
  <div class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8">
    <div class="grid gap-8 sm:grid-cols-2 md:grid-cols-3">
      {/* Brand */}
      <div>
        <p class="text-lg font-semibold text-white">Manchester River Cruises</p>
        {/* Tagline / logo TBC from Simon */}
      </div>

      {/* Contact details — placeholder values, confirm before launch */}
      <div>
        <h2 class="text-sm font-semibold uppercase tracking-wider text-slate-400">Contact</h2>
        <address class="mt-3 space-y-1 text-sm not-italic">
          <p>Salford Quays, Manchester, M50 3RB</p>
          <p>
            <a href="mailto:info@manchesterrivercruises.com" class="transition-colors hover:text-white">
              info@manchesterrivercruises.com
            </a>
          </p>
          <p>
            <a href="tel:+440000000000" class="transition-colors hover:text-white">Phone number TBC</a>
          </p>
        </address>
      </div>

      {/* Social links + Manage My Booking */}
      <div>
        <h2 class="text-sm font-semibold uppercase tracking-wider text-slate-400">Follow us</h2>
        <ul class="mt-3 flex flex-wrap gap-4 text-sm">
          {
            social.map((s) => (
              <li>
                <a
                  href={s.href}
                  class="transition-colors hover:text-white"
                  aria-label={`${s.label} (link TBC)`}
                >
                  {s.label}
                </a>
              </li>
            ))
          }
        </ul>
        <a
          href="/manage-booking"
          class="mt-5 inline-block text-sm font-medium text-amber-400 transition-colors hover:text-amber-300"
        >
          Manage My Booking
        </a>
      </div>
    </div>

    <div class="mt-10 border-t border-slate-800 pt-6 text-xs text-slate-400">
      <p>&copy; {year} Manchester River Cruises. All rights reserved.</p>
    </div>
  </div>
</footer>
